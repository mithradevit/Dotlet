
  # dotlet - shape builder (Copy)

  This is a code bundle for dotlet - shape builder (Copy). The original project is available at https://www.figma.com/design/um95sYClaVFPrkGkE983i9/dotlet---shape-builder--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  